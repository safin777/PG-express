<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Node Project</title>
  <link rel="stylesheet" href="../public/style.css">
  <script src="https://cdn.tailwindcss.com"></script>

</head>

<body class="body_container bg-black">
  <div class="container">
    <form action="/url" method="POST">
      <input type="text" class="url" name="given_url" placeholder="Put your Link">
      <button type="submit" class="text-green-500 border-2">Generate</button>
    </form>

    <div>
      <h2 class="text-white">Url Generated by <%= userinfo.firstName %></h2>
    </div>
    

    <div class="url_Container">
      <!-- create a table here -->
      <table class="table-auto">
        <thead>
          <tr>
            <th class="px-4 py-2 text-white">S.No</th>
            <th class="px-4 py-2 text-white">Short Url</th>
            <th class="px-4 py-2 text-white">Original Url</th>
            <th class="px-4 py-2 text-white">Clicks</th>
          </tr>
        </thead>
        <tbody>
            <% if(urls.length != 0) { %>
            <% urls.forEach((url, index) => { %>
              <tr>
                <td class="border px-4 py-2 text-white"><%= index + 1 %></td>
                <td class="border px-4 py-2 text-lime-400"><a href="<%= 'http://localhost:3000/url/'+url.shortId %>"><%= url.shortId %> </a></td>
                <td class="border px-4 py-2 text-white"><%= url.redirectUrl %></td>
                <td class="border px-4 py-2 text-white"><%= url.visitHistory.length %></td>
              </tr>
            <% }) %>
          <% } else { %>
            <tr>
              <td class="border px-4 py-2 text-white" colspan="4">No Urls Found</td>
            </tr>
          <% } %>
        </tbody>
    </div>
    
  </div>
</body>

</html>